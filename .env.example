# ============================================================================
# Redeem-O-Matic Environment Variables
# ============================================================================
# This file consolidates all environment variables used across the project.
# Copy this file to .env (backend) or .env.local (frontend) and fill in your values.
# Never commit actual .env files to version control.
#
# For backend: Create backend/.env
# For frontend: Create frontend/.env.local (React uses .env.local by default)
# ============================================================================

# ----------------------------------------------------------------------------
# Backend Environment Variables (Rails)
# ----------------------------------------------------------------------------

# Rails Environment
# Options: development, test, production
RAILS_ENV=development

# Rails Master Key (for encrypted credentials)
# Generate with: rails secret
# Required for production deployments
RAILS_MASTER_KEY=

# Secret Key Base (generate with: rails secret)
# Alternative to RAILS_MASTER_KEY for older Rails versions
# SECRET_KEY_BASE=your_secret_key_here

# Database Configuration
# Maximum number of database connections per pool
RAILS_MAX_THREADS=5

# Database URL (optional, overrides config/database.yml)
# For SQLite: DATABASE_URL=sqlite3:db/production.sqlite3
# For PostgreSQL: DATABASE_URL=postgresql://user:password@localhost/dbname
# DATABASE_URL=

# Database Host (for production with external database)
# Leave empty for SQLite (development/test)
# DB_HOST=

# ----------------------------------------------------------------------------
# CORS Configuration
# ----------------------------------------------------------------------------

# Allowed CORS origins (comma-separated)
# Example: http://localhost:3001,https://app.yourdomain.com
CORS_ORIGINS=http://localhost:3001

# For production, add your frontend domain:
# CORS_ORIGINS=http://localhost:3001,https://app.yourdomain.com

# ----------------------------------------------------------------------------
# Server Configuration (Puma)
# ----------------------------------------------------------------------------

# Number of worker processes (default: 1)
# Increase for better performance on multi-core systems
# WEB_CONCURRENCY=1

# Pidfile location (optional)
# PIDFILE=tmp/pids/server.pid

# ----------------------------------------------------------------------------
# Background Jobs (Solid Queue)
# ----------------------------------------------------------------------------

# Run Solid Queue Supervisor inside Puma process
# Set to 'true' to enable job processing in web server
# SOLID_QUEUE_IN_PUMA=true

# Number of concurrent jobs (default: 1)
# JOB_CONCURRENCY=3

# ----------------------------------------------------------------------------
# Logging
# ----------------------------------------------------------------------------

# Rails log level
# Options: debug, info, warn, error, fatal
# RAILS_LOG_LEVEL=info

# ----------------------------------------------------------------------------
# CI/CD
# ----------------------------------------------------------------------------

# Set to 'true' in CI environments to enable eager loading
# CI=false

# ----------------------------------------------------------------------------
# Deployment (Kamal)
# ----------------------------------------------------------------------------

# Docker registry password (for Kamal deployments)
# KAMAL_REGISTRY_PASSWORD=

# ----------------------------------------------------------------------------
# Frontend Environment Variables (React)
# ----------------------------------------------------------------------------

# Node Environment
# Options: development, production, test
# React sets this automatically, but you can override if needed
# NODE_ENV=development

# API Base URL
# Backend API endpoint for the frontend to connect to
# Default: http://localhost:3000/api/v1
REACT_APP_API_URL=http://localhost:3000/api/v1

# For production, use:
# REACT_APP_API_URL=https://api.yourdomain.com/api/v1

# ============================================================================
# Notes:
# ============================================================================
# - Backend variables are typically loaded using dotenv gem or Rails credentials
# - Frontend variables must be prefixed with REACT_APP_ to be accessible
# - React environment variables are embedded at build time
# - Never commit .env files with actual secrets
# - Use Rails encrypted credentials for production secrets:
#   rails credentials:edit --environment production
# ============================================================================
